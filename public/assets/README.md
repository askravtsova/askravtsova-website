# Assets

Put your own media here. The site uses only local paths (no WordPress or external links).

**video/**
- `hero.mp4` — hero background video (optional; if missing, hero uses poster image or background colour)

**images/**
- `hero-poster.jpg` — poster shown while hero video loads (or as fallback)
- `about.jpg` — about section photo
- `mother-mays.jpg` — Mother May's project image
- `heathers.jpg` — Heathers project image

**Optional**
- `resume.pdf` in `assets/` — linked from the About “read my resume” button

Paths in `index.html` match these names. Add or rename files as you like and update the HTML if needed.
